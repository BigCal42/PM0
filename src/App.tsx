import React from 'react';

const App: React.FC = () => {
  return (
    <main className="app">
      <h1>PM0 React App</h1>
      <p>Vite is now configured with a proper entry point.</p>
    </main>
  );
};

export default App;
